# セキュリティポリシー

Auto Shaper は個人利用を主眼にした OSS ツールです。脆弱性を見つけた場合は、
悪用を防ぐため **公開の Issue を立てずに** 以下の方法で報告してください。

## 報告方法

このリポジトリの **[Security] タブ → [Report a vulnerability]**（GitHub Private
Vulnerability Reporting）から報告してください。内容は報告者とメンテナのみが見られる
非公開のやり取りになります。

報告には可能な範囲で以下を含めてください。

- 再現手順(可能であれば最小構成)
- 想定される影響範囲(実データ漏洩・認証バイパス等)
- 発生環境(ブラウザ / Node.js バージョン / `npm run dev` か `npm run server` か)

## 対応方針

- 個人メンテナンスのプロジェクトのため、対応スピードの保証はできませんが、
  内容を確認した上で可能な範囲で速やかに対応します。
- 修正後、報告者の了承が得られればリリースノート等で謝辞を記載します(匿名希望も可)。

## 既知の設計上の注意点(脆弱性ではなく仕様)

以下は既知のトレードオフであり、単体での報告は不要です。詳細は
[README「公開ネットワークに `npm run server` を晒す場合の注意」](README.md#公開ネットワークに-npm-run-server-を晒す場合の注意)
を参照してください。

- `/api/suggest`・`/api/extract`(LLM 中継)はログイン必須、`/api/auth/login`・
  `/api/auth/signup`・LLM 中継とも簡易レート制限あり(`server/rateLimit.mjs`)。ただし
  プロセス内メモリでの実装のため、Cloud Functions のように複数インスタンスに増える
  環境ではインスタンスごとにしか効かない(公開時はリバースプロキシ側での対策も推奨)
- CORS の既定値は全オリジン許可(ローカル個人利用向け)

上記以外の脆弱性(例: 認証バイパス、他ユーザーのデータへのアクセス、保存データの
未サニタイズ出力によるインジェクション等)は通常の報告対象です。
